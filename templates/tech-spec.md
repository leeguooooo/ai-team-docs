# {Status Prefix} {方案名}

> 复制本模板新建技术方案文档。**保持文档薄**：决策主线 + 背景 + 链接，不复制 API/schema 细节（指向 repo 才是真相）。

---

## 元信息

- **状态**：`[WIP]` / `[Done]` / `[Hold]` / `[Deprecated]`
- **对应 PRD**：(必填。链接到 PRD Hub 中的 PRD；纯技术债务无 PRD 时填"无（内部技术债务整治）")
- **负责人**：
- **创建日期**：YYYY-MM-DD
- **最近更新**：YYYY-MM-DD
- **目标版本**：v?.?.?
- **相关 PR**：#xxxx
- **依赖 / 影响系统**：

## 1. 背景与目标

> 为什么做这件事？解决什么问题？衡量目标的指标是什么？
> 一段话说清楚动机，不要堆背景。

## 2. 范围与非目标（Non-goals）

**范围**：
- [ ]

**非目标**（明确不做的事，避免范围蔓延）：
-

## 3. 方案概要

> 主要技术方案。**只放决策主线**，复杂细节用 ADR 单独立项。
> 涉及具体代码 / 接口实现：链接到 repo 路径，不要复制代码片段。

### 3.1 关键决策

- 决策 1：...，原因：...
- 决策 2：...，原因：...

> 重大决策记到 reference/ADR Log

### 3.2 备选方案对比（可选）

| 方案 | 优点 | 缺点 | 选 / 不选 |
|------|------|------|-----------|
| A | | | |
| B | | | |

## 4. API 契约

> 不要复制完整 OpenAPI YAML。列出新增/修改的 endpoint 即可，链接 repo `api/openapi.yaml`。

| Method | Path | 说明 | 鉴权 |
|--------|------|------|------|
| POST | `/api/...` | | Bearer |

详细字段定义见 `api/openapi.yaml` 中对应 schema。

## 5. 数据模型变化

> 不要在这里贴完整字段定义。指向 `reference/tables/<表名>` 页面，本节只写"差异"。

- **新表**：见 `reference/tables/<新表名>`
- **改表**：在 `reference/tables/<表名>` 中已记录字段变更
- **GSI / 索引变更**：

迁移方式：
- [ ] 双写
- [ ] 灰度
- [ ] 一次性切换
- [ ] 不需要迁移

## 6. 风险与回滚

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| | 低/中/高 | 低/中/高 | |

**回滚方案**：
- 配置开关：`FEATURE_X_ENABLED=false`
- 数据回退：

## 7. 上线计划

- [ ] Feature flag 接入：`<flag_name>`
- [ ] 灰度策略：先 X% → 50% → 100%，每阶段观察 X 小时
- [ ] 监控指标：
  - 错误率：
  - 业务指标：
- [ ] 告警规则：
- [ ] 文档更新：相关 reference 页面、术语表
- [ ] 通知：

## 8. 验收标准

- [ ] 关键 case 1
- [ ] 关键 case 2
- [ ] 性能指标：
- [ ] 兼容性：

## 9. 上线后回顾（可选）

> 上线后 1-2 周回头填这一节，把实际指标和预期对比，作为决策反馈。

- 实际效果：
- 与预期偏差：
- 经验教训：
